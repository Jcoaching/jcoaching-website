# エンジニア向け引き継ぎ

## 依頼内容と現在地

完成したJ Coachingの紹介サイトをGitHubで管理し、取得済みの `jcoaching.jp` で利用できるようにすることが今回の依頼です。既存の白基調のデザインとコンテンツを引き継いでください。

2026年9月15日の確認時点で、既存サイトは公開設定です。`jcoaching.jp` はSites側に登録済みですが、ドメイン認証・SSLは確認待ちです。DNS管理サービスと管理画面へのアクセスは未共有です。GitHubにはサイト一式と本書を登録しています。DNSの変更は行っていません。

## 実装の要点

- 静的な1ページサイト。アプリ用サーバー、データベース、CMS、決済、問い合わせフォームはありません。
- `dist/` がソース兼配信ディレクトリです。生成物として消したり、`.gitignore` に追加しないでください。
- 英語本文はHTML、日本語訳は `app.js` の `translations` にあります。新しい本文を増やす場合は `data-i18n` のキーと日本語訳を合わせて追加してください。
- 初期言語は英語。選択言語は `localStorage` の `jcoaching-language` に保存します。
- ページタイトルとdescriptionは言語切り替え時に更新します。日本語専用URLやhreflangによる言語別ページは未実装です。
- モバイルメニュー、月別プランのタブ、スクロール時の表示アニメーションがあります。FAQはHTMLの `details` / `summary` です。
- 写真の一部は元のスクリーンショットをCSSの比率・位置指定で切り取って表示しています。`source-crop` と画像のインラインstyleも確認してください。
- 外部フォント、npmパッケージ、分析タグは使用していません。

## 相談・SNS導線

| 用途 | 設定値・ルール |
| --- | --- |
| 無料相談・プラン相談 | https://wa.me/message/FWGC7BWRQIPJC1 |
| Instagram | https://www.instagram.com/jcoaching_sota/ |
| 短い生徒の声の動画 | https://youtu.be/x6UmHrxnJnk / https://youtu.be/a3Rw8I9yS4Q |

相談リンクは9箇所すべてWhatsAppへ直接遷移します。コピー用メッセージ、メッセージ入力済みURL、相談モーダルは不要というオーナーの指定です。

動画は再生ボタンを押したときだけ `youtube-nocookie.com` のiframeを挿入します。外部のYouTube視聴リンクもあります。移設先でCSPを設定する場合は、この埋め込み先と現在のCSSの使用方法に対応させてください。

## 維持するコンテンツ方針

- 対象は日本で働く外国人、主にN3〜N1。3ヶ月のプランを中心に紹介しています。
- 料金の具体額は掲載しません。目標・生活状況・レベル確認の後にプランを提案し、相談時に期間・サポート・総額を説明する流れです。
- Instagramを主な情報発信先として扱います。4.8万人の表示は2026年9月14日のスクリーンショットに基づく表示で、リアルタイムのフォロワー数ではありません。
- Nidhalさん・Farrelさんの長い面談動画とそのリンクは削除済みです。再掲載する場合は、オーナーの指定どおり1〜2分に編集した動画を使ってください。現在、短縮版はありません。
- 二人の紹介は学習目標とプランの例です。達成済みの結果として書き換えないでください。
- 白・深いグリーン・控えめなゴールドを基本に、アイボリーの背景と細いラインで整えた現状のデザインを維持してください。

## 素材について

同梱画像14点は現在の公開用ファイルです。生徒レビューの写真、サービス紹介イラスト、Instagram画面、代表の桜の写真はオーナー提供の掲載可素材です。レビュー本文は提供資料をもとにしています。

営業資料に含まれていた多数の生徒の顔が写る集合画像は使用しない指定です。現在のパッケージには含めていません。以前のGit履歴や資料から復活させないでください。Nidhalさん・Farrelさんの顔の静止画像も、このパッケージにはありません。

街並みと学習机の画像は雰囲気を伝える生成画像で、実際の教室の写真ではありません。契約書、顧客カルテ、個別の契約金額、個人連絡先、元の営業資料、認証情報は同梱していません。

## jcoaching.jpの設定

以下は既存のChatGPT Sitesに接続する場合に必要な設定です。ホスティングを移す場合は、移設先が指定するDNSレコードに従ってください。GitHubにコードを置くだけなら、このDNSの接続先を変更する必要はありません。

| 種別 | ホスト名（完全名） | 値 |
| --- | --- | --- |
| A | jcoaching.jp | 162.159.143.30 |
| A | jcoaching.jp | 172.66.3.26 |
| TXT | _openai-site-verification.jcoaching.jp | openai-site-verification=gSEUxtQGo9csxuPVaPdGIvYGs6_Yrl_R2GIc7THoCWs |
| TXT | _cf-custom-hostname.jcoaching.jp | 644bd517-9b98-4b9b-bea7-4e530f1f4824 |

DNS管理画面がドメイン名を自動で末尾に付ける形式の場合は、ホスト欄には対応するラベルだけを入力します。ルートの表記はサービスによって空欄または `@` など異なります。現在のレコードを確認し、サイト用のA/AAAAレコードの競合を解消してください。メール用MX・TXTなど、他の用途の設定は維持してください。

上記は2026年9月15日にSitesから取得した値です。DNS入力後はSites側で状態を再確認し、ドメインとSSLが有効になってからHTTPSで表示を確認してください。`www.jcoaching.jp` は登録していません。

現在のHTMLのcanonicalは `https://j-coaching.sota27310726.chatgpt.site` です。`https://jcoaching.jp/` で正常に公開できた段階で、正規URLをそちらへ更新してください。必要に応じてOG URL・OG画像・sitemap・robotsの方針も整理してください。これらの追加設定は現時点では未実施です。

## 引き継ぎ時の確認項目

1. GitHubの所有者はJcoaching、リポジトリ名はjcoaching-website、可視性はPrivate。エンジニアのGitHubユーザー名を確認し、必要なメンバーにアクセス権を付ける。
2. GitHubからcloneし、READMEの手順で起動する。過去のSitesのGit履歴は取り込まない。
3. `dist/` をローカル配信し、スマホ・PCで画像、改行、メニュー、FAQ、タブ、言語切り替えを確認する。
4. 相談ボタン9箇所が指定のWhatsAppへ直接遷移すること、2本の短い動画が再生できることを確認する。
5. ホスティングの運用先と、GitHubから公開先へ反映する方法を決める。現在、自動デプロイは設定されていない。
6. ドメイン管理画面でDNSを設定し、SSLの有効化とjcoaching.jpでの表示を確認する。
7. 正規URLを更新し、変更担当・更新手順を決める。

## この引き継ぎで実施した確認

公開ソースとのファイル一致、JavaScript構文、画像参照、アンカー、日英の翻訳キー、相談リンク、削除対象の長い動画が含まれないことを確認しています。引き継ぎパッケージに対する新たなブラウザでの表示確認は実施していません。
